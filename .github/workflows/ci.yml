name: CI Security Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
  workflow_dispatch:

jobs:
  security-checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Conftest
        run: |
          curl -L https://github.com/open-policy-agent/conftest/releases/download/v0.69.0/conftest_0.69.0_Linux_x86_64.tar.gz -o conftest.tar.gz
          tar xzf conftest.tar.gz
          sudo mv conftest /usr/local/bin/

      - name: Run Policy Checks
        run: |
          conftest test Dockerfile --policy policies/docker
          conftest test k8s/deployment.yaml --policy policies/kubernetes

      - name: Install Syft
        run: |
          curl -L https://github.com/anchore/syft/releases/download/v1.0.0/syft_1.0.0_linux_amd64.tar.gz -o syft.tar.gz
          tar xzf syft.tar.gz
          sudo mv syft /usr/local/bin/

      - name: Generate SBOM
        run: |
          ./syft . -o cyclonedx-json > sbom/sbom-cyclonedx.json

      - name: Install Trivy
        run: |
          curl -L https://github.com/aquasecurity/trivy/releases/download/v0.50.0/trivy_0.50.0_Linux-64bit.tar.gz -o trivy.tar.gz
          tar xzf trivy.tar.gz
          sudo mv trivy /usr/local/bin/

      - name: Analyze SBOM
        run: |
          ./trivy sbom sbom/sbom-cyclonedx.json

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/myapp:1.0

      - name: Install Cosign
        run: |
          curl -L https://github.com/sigstore/cosign/releases/download/v2.2.0/cosign-linux-amd64 -o cosign
          chmod +x cosign
          sudo mv cosign /usr/local/bin/
          
      - name: Write Cosign Keys from Secrets
        run: |
          echo "${{ secrets.COSIGN_KEY }}" > cosign.key
          echo "${{ secrets.COSIGN_PUB }}" > cosign.pub

      - name: Sign Docker Image
        run: |
          ./cosign sign --key cosign.key ${{ secrets.DOCKERHUB_USERNAME }}/myapp:1.0

      - name: Verify Docker Image
        run: |
          ./cosign verify --key cosign.pub ${{ secrets.DOCKERHUB_USERNAME }}/myapp:1.0

